{
  "name": "teammatcher",
  "version": "1.0.0",
  "description": "Team creating website",
  "main": "dist/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc && cd dist && ls && npm run copy-files",
    "copy-files": "cp -r ./src/views ./dist/ && cp -r ./src/public ./dist/"
  },
  "author": "Luis Martinez",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.3",
    "handlebars": "^4.7.2",
    "handlebars-helpers": "^0.10.0",
    "hbs": "^4.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "eslint": "^7.32.0",
    "nodemon": "^2.0.2",
    "ts-node": "^10.6.0"
  }
}
